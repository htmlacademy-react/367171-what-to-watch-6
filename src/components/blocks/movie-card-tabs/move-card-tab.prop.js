import PropTypes from 'prop-types';

const movieCardTabPropTypes = {
  name: PropTypes.string,
  genre: PropTypes.string,
  released: PropTypes.number,
  runTime: PropTypes.number,
  id: PropTypes.number,
};

export default movieCardTabPropTypes;
